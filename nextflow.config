process {
  // Fix error strategy to retry on exit code 137 (out of memory for plotHeatmap).
  errorStrategy = { task.exitStatus in ((130..145) + 104 + 125) ? 'retry' : 'finish' }
}

profiles {
  ribosomal_dna {
    includeConfig "./conf/ribosomal_dna.config"
  }
}
